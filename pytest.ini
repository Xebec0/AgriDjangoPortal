[pytest]
DJANGO_SETTINGS_MODULE = agrostudies_project.settings
python_files = test_*.py
testpaths =
    tests

addopts =
    -ra
    --strict-markers

markers =
    slow: slow tests (deselect with '-m \"not slow\"')
